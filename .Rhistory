import numpy as np
import random
from statistics import mean
def trader(n_enchantments = 36, max_n = 100):
trades = [] # an empty set
for i in range(max_n):
# is it an enchantment trade?
# there is a 50% chance
random_trade = random.randint(0, 1)
if random_trade == 1:
# is the trade for a new book?
random_trade = random.randint(0, n_enchantments - 1)
trades.append(random_trade)
def trader(n_enchantments = 36, max_n = 100):
trades = [] # an empty set
for i in range(max_n):
# is it an enchantment trade?
# there is a 50% chance
random_trade = random.randint(0, 1)
if random_trade == 1:
# is the trade for a new book?
random_trade = random.randint(0, n_enchantments - 1)
if random_trade not in trades:
trades.append(random_trade)
return len(trades) # out of n_enchantments
for i in range(0, 300, 10):
# simulate rolling 10 times
rolls = [trader(n_enchantments = 36, max_n = i) for _ in range(10)]
print(f"{i}: {mean(rolls)})")
rolls_list[i] = mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)])
rolls_list = []
for i in range(0, 300, 10):
# simulate rolling 10 times
rolls_list[i] = mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)])
rolls_list = []
for i in range(0, 300, 10):
# simulate rolling 10 times
rolls_list.append(mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)]))
import matplotlib
import matplotlib
plot(rolls_list)
matplotlib.pyplot.plot(rolls_list)
import matplotlib.pyplot as plt
plt(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
plt.plot(rolls_list)
plt.show()
rolls_list = []
x = []
for i in range(0, 300, 10):
x.append(i)
# simulate rolling 10 times
rolls_list.append(mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)]))
rolls_list = []
x = []
for i in range(0, 300, 10):
x.append(i + 1)
# simulate rolling 10 times
rolls_list.append(mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)]))
plt.plot(x, rolls_list)
plt.title('Basic Line Plot')
plt.show()
plt.plot(x, rolls_list)
plt.title('Basic Line Plot')
plt.show()
plt.clf()
plt.plot(x, rolls_list)
plt.title('Basic Line Plot')
plt.show()
plt.clf()
plt.plot(x, rolls_list)
plt.title('Avg. X Number of Rolls to Recieve Y Unique Enchantments')
plt.show()
plt.clf()
import numpy as np
import random
from statistics import mean
import matplotlib.pyplot as plt
def trader(n_enchantments = 36, max_n = 100):
trades = [] # an empty set
for i in range(max_n):
# is it an enchantment trade?
# there is a 50% chance
random_trade = random.randint(0, 1)
if random_trade == 1:
# is the trade for a new book?
random_trade = random.randint(0, n_enchantments - 1)
if random_trade not in trades:
trades.append(random_trade)
return len(trades) # out of n_enchantments
rolls_list = []
x = []
for i in range(0, 300, 10):
x.append(i + 1)
# simulate rolling 10 times
rolls_list.append(mean([trader(n_enchantments = 36, max_n = i) for _ in range(10)]))
plt.plot(x, rolls_list)
plt.title('Avg. X Number of Rolls to Recieve Y Unique Enchantments')
plt.show()
plt.clf()
import numpy as np
import random
from statistics import mean
import matplotlib.pyplot as plt
def trader(n_enchantments = 36, max_n = 100):
trades = [] # an empty set
for i in range(max_n):
# is it an enchantment trade?
# there is a 50% chance
random_trade = random.randint(0, 1)
if random_trade == 1:
# is the trade for a new book?
random_trade = random.randint(0, n_enchantments - 1)
if random_trade not in trades:
trades.append(random_trade)
return len(trades) # out of n_enchantments
rolls_list = []
x = []
for i in range(0, 300, 10):
x.append(i + 1)
# simulate rolling 10 times
rolls_list.append(mean([trader(n_enchantments = 36, max_n = i) for _ in range(100)]))
plt.plot(x, rolls_list)
plt.title('Avg. X Number of Rolls to Recieve Y Unique Enchantments')
plt.show()
plt.clf()
np.geometric(20)
np.random.geometric(20)
np.random.geometric(0.1)
def new_enchant_prob(i, n):
# where i is the ith enchantment and n is the total number of possible enchantments
return np.random.geometric(n / (n - i + 1))
prob_list = []
for i, n in zip(rolls_list, 36):
prob_list.append(new_enchant_prob(i, n))
def new_enchant_prob(i, n):
# where i is the ith enchantment and n is the total number of possible enchantments
return np.random.geometric(n / (n - i + 1))
prob_list = []
for i in rolls_list:
prob_list.append(new_enchant_prob(int(i), 36))
new_enchant_prob(10, 20)
def new_enchant_prob(i, n):
# where i is the ith enchantment and n is the total number of possible enchantments
return np.random.geometric((n - i + 1) / n)
prob_list = []
new_enchant_prob(10, 20)
for i in rolls_list:
prob_list.append(new_enchant_prob(int(i), 36))
np.random.geometric(11/20)
rolls_list
np.random.geometric(rolls_list[3]/36)
np.random.geometric(rolls_list[5]/36)
np.random.geometric(rolls_list[10]/36)
rolls_list[10]/36)
rolls_list[10]/36
(36 - rolls_list[10] + 1)/36
np.random.geometric((36 - rolls_list[10] + 1)/36)
